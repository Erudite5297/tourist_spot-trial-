document.getElementById("upload-form").addEventListener("submit", function(event) {
    event.preventDefault();
    alert("Your photo and comment have been submitted!");
    // Handle the file upload and comment submission here
});
